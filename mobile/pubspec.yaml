name: mobile
description: A mock e-commerce app made with Flutter and Java Spring Boot.

environment:
  sdk: '^3.11.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^10.0.0  # <-- Bumped to v10
  provider: ^6.1.1
  go_router: ^13.2.0
  google_fonts: ^6.2.1
  jwt_decoder: ^2.0.1
  lucide_icons: ^0.257.0

# ADD THIS BLOCK: This surgically removes the broken "jni" package
# by forcing the reliable version of path_provider_android.
dependency_overrides:
  path_provider_android: 2.2.12